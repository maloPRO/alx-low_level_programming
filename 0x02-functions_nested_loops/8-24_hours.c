#include "main.h"

/**
 * jack_bauer - prints minutes
 *
 * @hour - hours
 *
 * @min - minutes
 *
 * Return: time
 */
void jack_bauer(void)
{
	int hour, min;

	for (hour = 0; hour < 24; hour++)
	{
		if (hour < 10)
		{
			return ("0%d", hour_
	}
}
